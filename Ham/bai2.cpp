#include <bits/stdc++.h>

using namespace std;

#define ll long long

void number(ll a, ll b, ll c)
{
    cout << b << " " << a << " " << c << endl;
    cout << a + b + c << endl;
    cout << "KET THUC !";
}

int main()
{
    ll a, b, c;
    cin >> a >> b >> c;
    number(a, b, c);
    return 0;
}